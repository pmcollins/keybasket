<%= breadcrumbs([@company, @property, @unit, 'Edit Phone']) %>

<fieldset>
  <legend>Edit Unit Phone</legend>
  <% form_for(
    :phone,
    :url => m([@company, @property, @unit, @phone]),
    :builder => LabeledBuilder,
    :html => {:method => :put}
  ) do |form| %>
    <%= render :partial => 'form', :object => form %>
    <%= submit_tag 'Save' %>
  <% end %>
</fieldset>

<fieldset>
  <legend>Delete Phone</legend>
  <% form_for(
    :phone,
    :url => m([@company, @property, @unit, @phone]),
    :html => {:method => :delete}
  ) do |form| %>
    <%= submit_tag 'Delete' %>
  <% end %>
</fieldset>
