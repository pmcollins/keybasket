<%= breadcrumbs([@company, @property, @unit, @rental_agreement, @tenant, 'New Phone']) %>

<fieldset>
  <legend>New Tenant Phone</legend>
  <% form_for(
    :phone,
    :url => m([@company, @property, @unit, @rental_agreement, @tenant, 'tenant_phones']),
    :builder => LabeledBuilder
  ) do |form| %>
    <%= render :partial => 'form', :object => form %>
    <%= submit_tag 'Save' %>
  <% end %>
</fieldset>

